//not optimized

long numberOfPaths(int m, int n)
    {
        // code here
        if (m == 1 || n == 1)
            return 1;
        return numberOfPaths(m - 1, n) + numberOfPaths(m, n - 1);
    }
