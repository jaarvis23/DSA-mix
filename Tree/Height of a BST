int Height(Node root)
    {
        if(root==null)
            return 0;
        int l = CountNodes(root.left);
        int r = CountNodes(root.right);
        if(l>=r)
            return l+1;
        else
            return r+1;
        
    }
